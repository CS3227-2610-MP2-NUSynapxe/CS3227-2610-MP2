package nusynapxe.domain;

/** Administrative sex values recorded for a patient. */
public enum Sex {
  /** Female. */
  FEMALE,
  /** Male. */
  MALE,
  /** Another recorded value. */
  OTHER,
  /** Patient declined to state the value. */
  UNDISCLOSED
}
