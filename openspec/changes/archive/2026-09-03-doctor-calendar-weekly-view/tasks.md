# Implementation Tasks

## 1. Calendar data model and persistence

- [x] 1.1 Define the calendar preference and working-interval value objects, including the preferred first day, enabled days, one or more daily intervals, display defaults, half-hour editor increments, midnight end representation, and validation for positive non-overlapping intervals. Verification: add unit tests covering default construction, disabled days, breaks/split shifts, boundary values from 00:00 through 24:00, and rejected overlaps or invalid ranges.
- [x] 1.2 Extend the SQLite schema with the version-5 doctor calendar settings and working-interval tables, constraints, and useful lookup indexes. Verification: schema tests migrate a version-4 database and create a fresh database, assert the new tables and default profiles for existing doctors, and confirm appointment rows and statuses are unchanged.
- [x] 1.3 Implement transactional repository load and whole-snapshot save behavior for doctor calendar settings. Verification: repository tests round-trip first-day settings, disabled days, multiple intervals, and removed breaks; a failed validation or persistence operation leaves the prior snapshot intact and no work-location field is stored.

## 2. Authorized calendar read and settings services

- [x] 2.1 Add an appointment range query for a doctor that returns every appointment overlapping the selected seven-day interval, including appointments crossing the range boundary and cancelled appointments. Verification: repository tests cover start/end boundary semantics, chronological ordering, status retention, and exclusion of another doctor's appointments.
- [x] 2.2 Add a calendar-facing service API that loads the signed-in doctor's week and settings, returns only administrative patient display data, and enforces doctor ownership for reads and saves. Verification: service tests prove a doctor can access only their own calendar, clinical information is absent from the projection, unauthorized access is rejected, and empty weeks are returned successfully.
- [x] 2.3 Wire the calendar service and its repositories through the existing clinic service composition without changing appointment mutation rules. Verification: service-construction tests or an application-context test can create the calendar dependencies, and existing appointment scheduling/rescheduling tests continue to pass.

## 3. Calendar calculations and layout contracts

- [x] 3.1 Implement a week model that resolves a date using the saved first-day preference, moves exactly seven days for adjacent-week navigation, formats the selected range and week number, and uses Singapore local time for Today. Verification: unit tests cover Sunday and Monday starts, month/year boundaries, dates at the edge of a week, and the current-week calculation with an injected clock.
- [x] 3.2 Implement pure time-grid shading calculations for past dates, elapsed time today, disabled days, before/after-hours regions, and gaps created by breaks. Verification: fixed-clock tests assert the correct shaded/unshaded portions for current, past, and future weeks and prove that changing display intervals cannot remove an appointment from the returned event set.
- [x] 3.3 Implement appointment block placement and overlap lane allocation for a seven-day grid, retaining blocks outside working hours and making cancelled blocks identifiable and visually muted. Verification: unit tests cover overlapping appointments, same-time cancelled appointments, cross-midnight or week-boundary clipping, and all existing `AppointmentStatus` values.

## 4. Doctor navigation and weekly Calendar page

- [x] 4.1 Add a separate Calendar destination to the doctor navigation while preserving Dashboard and Patients behavior and the existing dashboard appointment actions. Verification: DoctorView/TestFX tests locate stable navigation controls, open the Calendar page, return to the other pages, and confirm the selected doctor scope is retained.
- [x] 4.2 Build the calendar toolbar with Today, previous-week, next-week, selected-range display, week-picker trigger, and settings icon controls. Verification: UI tests assert that Today returns to the Singapore current week, arrows move by one week, the range text and week number refresh, and the settings control opens Calendar settings.
- [x] 4.3 Build the custom week-picker popup shown by the reference design: month calendar with week numbers and selected-week highlighting, month navigation, year selector/month grid, and Today action. Verification: TestFX or focused control tests open and close the popup, navigate months and years, select a date or week row, update the calendar, preserve the preferred week start, and expose labels/keyboard focus for each interactive control.
- [x] 4.4 Render the scrollable seven-day time grid with day/date headers, Singapore time labels, hour and half-hour guides, grey shading layers, administrative appointment details, and status-aware blocks. Verification: UI tests assert the seven columns, empty-week state, patient name and appointment times, readable status text for every lifecycle state, visibility of an outside-hours appointment, and absence of clinical details.
- [x] 4.5 Add the current-time line and its lifecycle behavior, plus non-color cues for elapsed, disabled, outside-hours, and appointment-status states. Verification: fixed-clock UI tests place the line at the expected current-day offset, verify it is absent for past/future weeks, advance the clock to confirm updates, and dispose the page without leaving a timer running; accessibility-focused tests check roles, labels, and text cues.

## 5. Calendar settings page

- [x] 5.1 Build the doctor Calendar settings page with a first-day-of-week selector, fixed read-only Singapore timezone label, and one editable working-hours row per weekday. Verification: UI tests find the settings page, confirm there is no work-location control, load saved/default values, switch the first day, and enable or disable individual days.
- [x] 5.2 Add multiple start/end interval editors with Add interval and Remove actions so a doctor can represent breaks such as lunch and split shifts. Verification: UI tests add, edit, and remove a break, confirm disabled days do not expose editable intervals, and verify midnight end and half-hour options are represented correctly.
- [x] 5.3 Implement settings save, cancel/back, validation feedback, persistence, and calendar refresh behavior. Verification: integration/UI tests save valid settings and observe updated week ordering and shading after returning to Calendar; cancel discards drafts; invalid, overlapping, or empty enabled-day intervals are rejected with an accessible message and do not overwrite the saved snapshot.

## 6. Regression coverage and delivery documentation

- [x] 6.1 Add regression coverage proving working hours and breaks are visual preferences only: appointment booking and rescheduling outside configured hours remain allowed when existing appointment/time-off conflict rules permit them, and settings changes do not mutate appointment state. Verification: service-level tests pass for outside-hours scheduling, conflict enforcement remains unchanged, and appointment statuses/timestamps are identical before and after a settings save.
- [x] 6.2 Add authorization and confidentiality regression coverage across the Calendar page, settings page, week query, and patient projection. Verification: tests prove a doctor cannot read or save another doctor's settings or appointments and no clinical record fields are rendered or returned by the calendar API.
- [x] 6.3 Update the user/developer documentation to describe the separate doctor Calendar page, custom week picker, visual-only working hours, breaks, settings, and navigation controls. Verification: documentation review traces each added behavior to `openspec/changes/doctor-calendar-weekly-view/specs/doctor-calendar/spec.md` and includes the no-work-location and non-blocking constraints.
- [x] 6.4 Run the repository quality gate and OpenSpec validation after implementation, resolving any failures before marking the change complete. Verification: `.\gradlew.bat check --offline --no-daemon --console=plain`, `git diff --check`, and `openspec validate --change doctor-calendar-weekly-view --strict` complete successfully, with the relevant unit/UI test reports retained.
